'use strict';
describe('app', function(){
    var routeService;

    beforeEach(function(){
        module('chatbot');

        inject(function($route) {
            routeService = $route;
        })
    });

    //Add tests here
});